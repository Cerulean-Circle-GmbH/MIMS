version: '3'

services:
  once-ubuntu-22-04-sshd:
    container_name: ${DOCEKR_CONTAINER_NAME}
    image: ${DOCEKR_IMAGE}
    #restart: unless-stopped
    volumes:
      - ./../../../../../src/sh/init:/root/init
      - ./../../../../../src/sh/init:/home/ubuntu/init
    ports:
      - "80:80"
      - "443:443"
      - "8022:22"
    command: "tail -f /root/.bashrc "
# networks:
#   default:
#     external:
#       name: once-woda-network